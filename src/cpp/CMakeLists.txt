cmake_minimum_required(VERSION 3.2)
project(computer-science-interview-cpp
        LANGUAGES CXX
        VERSION 0.1)


add_executable(sizeof sizeof.cpp)

# install(TARGETS sizeof RUNTIME DESTINATION bin)
