project(computer-science-interview-design_pattern
        LANGUAGES CXX
        VERSION 0.1)


add_subdirectory(singleton)
add_subdirectory(observer)

# install(TARGETS sizeof RUNTIME DESTINATION bin)
